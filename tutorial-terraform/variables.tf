// 3. Archivo de variables
// Define aquí tus variables
// Las variables pueden ser input o output
// Se pueden definir variables para evitar escribir a mano valores en los archivos
// var.nombre_variable

// Si ejecutas
// $ terraform plan/apply
// Se cargaran todas las variables. Si la variables
// se pasa por comando (-var) y no se añaden, se utilizarán los valores por defecto
// $ terraform plan -var instance_type=t2.large
